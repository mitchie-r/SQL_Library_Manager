extends layout

block content
  h1= title

  form(method="post")
    if errors
      h2.errors Error...!
      ul.errors
        each error in errors
          li= error.message

    include form

    p 
      input(type="submit" value="Update Book")
      a.button(href="/books") Cancel